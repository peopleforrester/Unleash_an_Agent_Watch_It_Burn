# Changelog

All notable changes to this project will be documented in this file.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added
- (2026-06-25) Recorded three live-verification findings from Michael's execution of the Falco alerts milestone into the observability meta-PRD decision log: (1) the Falco `in_agent_container` macro must use the `kagent` pod label key, not `app`, because the kagent operator labels the agent pod with `kagent=workshop-agent`; any future Falco rule targeting that pod uses this key; (2) Falcosidekick chart 0.14.0 reads `extraEnv` at `config.extraEnv` (not root level), a silent misconfiguration that dropped the Datadog API key; (3) the Datadog Event Stream acceptance check is manual-only — the app key is not deployed to the cluster, so automated scripts cannot query the Events API.
- (2026-06-25) Hardened PRD #26 (Datadog Agent DaemonSet) against four CodeRabbit findings: added operator chart version pinning instruction (no "latest stable"), added an ExternalSecret ordering note clarifying that the first DatadogAgent CR reconcile may fail while ESO populates the secret (auto-resolves), added an explicit Talon auto-remediation verification gate to the Milestone 4 acceptance checklist, and replaced a stale `research/NN` placeholder with the actual file `research/30`. Also resolved unresolvable `…` shorthand research filenames in Milestone 6–8 Step 0 read lists so a cold AI can locate each file by name.
- (2026-06-24) Completed the Milestone 5 design conversation for the observability plan: locked nine decisions covering how the Datadog Agent DaemonSet is deployed, which components get named integrations, and how Kyverno exports its policy-decision traces. Key decisions: deploy via Datadog Operator (two ArgoCD Applications — Operator in sync-wave "3", DatadogAgent CR in sync-wave "4"); `spec.features.*` keys in the DatadogAgent CR (not Helm `datadog.*`); log collection and Prometheus scrape ON; APM OFF (OTel traces reach APM via Collector path); named integrations wired for ArgoCD, Falco, cert-manager, and Istio ambient (L4-only; optional waypoint tracked in issue #25); Kyverno uses native OTLP (`otelConfig: grpc`) instead of an Agent Prometheus check; resource footprint locked (Node Agent 200m/256Mi, Process Agent 100m/200Mi, Cluster Agent 200m/256Mi); `clusterName: watch-it-burn`; EKS+CloudWatch skipped; eight components not wired (KubeArmor, ESO, Backstage, evil-mcp-shim, customer-stream, and three Collector-path AI-layer components). `datadog-secret` created via ESO ExternalSecret with `api-key` and `app-key` fields. Created issue #24 (Datadog community dashboard research spike, gates M7) and issue #25 (optional Istio L7 waypoint). Produced child PRD #26 (`prds/26-datadog-agent-daemonset-named-integrations.md`). ROADMAP updated.
- (2026-06-24) Completed the Milestone 4 design conversation for the observability plan: locked three decisions covering how Falco runtime alerts reach Datadog when exfil or abuse is attempted in the workshop. The Falcosidekick→Datadog Event Stream path is already wired in the manifest and confirmed correct for US1; two verify-at-build tasks remain for Michael (secret namespace + live event flow). Both the Falcosidekick path (Event Stream) and the Datadog Agent named integration (Log Explorer + OOTB dashboard) will be wired — they feed different Datadog surfaces with no duplication. Three specific Falco rules must be visible in Datadog for the two execution Challenges: "Shell or Exec In Workshop Agent Pod" (any exec in the agent pod fires this — `ls`, `grep`, and a fork bomb all trigger it for the same structural reason, which is the teaching point), "Sensitive File Access" (fires when the canary credential file is read — renamed from the previous "Read Of Planted Fake Secret By Workshop Agent" which broke the workshop illusion and assumed attribution Falco doesn't have), and "Fork Bomb In Workload Container". Corrected the Falco named Agent integration description in research files (was incorrectly described as metrics-only; it collects individual alert logs AND aggregate metrics). Produced child PRD #23 (`prds/23-falco-runtime-alerts-datadog.md`). ROADMAP updated.
- (2026-06-22) Created the observability meta-PRD (GitHub issue #7, `prds/7-observability-meta.md`): 8 self-contained milestones that each produce a research synthesis or a child PRD covering one area of the observability strategy for the Watch It Burn workshop. Each milestone is executable by a cold AI instance — it lists its own reads, design decisions, and child-PRD creation steps. Decisions are documented in each child PRD's Decision Log rather than separate planning docs.
- (2026-06-22) Created `prds/` directory to hold PRD files; added a local `.gitignore` override since the global gitignore excludes `prds/`.
- (2026-06-22) Created `docs/ROADMAP.md` with forward-looking implementation order for observability PRDs.
- (2026-06-24) Completed the Milestone 2 design conversation for the observability plan: resolved eight decisions covering how to migrate the AI layer off Michael's custom `witb_*`/`tier` conventions onto OTel GenAI semconv (`gen_ai.*`) visible in Datadog LLM Observability. Key decisions: single shared OTel Operator Instrumentation CRD for all custom Python pods (not per-component), config-only native gen_ai tracing for kagent/ADK and agentgateway (no extra libraries), `OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=EVENT_ONLY` (not `true`, which silently collects nothing), keep `witb_cost_usd` with a `model` label (retire `witb_tokens_total`, `witb_requests_total`, `tier` label), Weaver `registry check` in CI with `live-check` as a manual acceptance step. Produced child PRD (#20, `prds/20-otel-genai-semconv-migration.md`) with seven milestones, a locked-decisions table, and a build verification checklist. ROADMAP updated.
- (2026-06-23) Research spike #9 → `research/28-datadog-llm-obs-otlp-2026.md`: Datadog LLM Observability OTLP ingestion path (native `gen_ai.*` ingest, semconv v1.37+, ingestion paths, `CAPTURE_MESSAGE_CONTENT` enum, ADK native emission). Adversarially validated (19 claims confirmed); one open seam (Collector→LLM-Obs auto-routing) flagged verify-at-build.
- (2026-06-23) Research spike #10 → `research/29-python-ai-instrumentation-2026.md`: per-component Python AI-layer instrumentation (kagent/ADK, agentgateway v1.3.0 config-file-only tracing, guard-proxy manual OTel SDK spans, OpenLLMetry status). Validated (11 confirmed; ADK content-capture corrected to `EVENT_ONLY`).
- (2026-06-23) Research spike #11 → `research/30-per-component-telemetry-synthesis-2026.md`: telemetry synthesis across all 13 components. Validated; corrected research/18 (ArgoCD has a DD OOTB dashboard; cert-manager does not; Istio ambient autodiscovery is manual). No wire-or-skip decisions (deferred to Milestone 5).
- (2026-06-23) Research spike #12 → `research/31-guard-proxy-sanitization-tracing-2026.md`: before/after sanitization tracing for guard-proxy (content on span events; `CAPTURE_MESSAGE_CONTENT` is a util-genai/contrib property, not honored by hand-written spans). Validated; before/after Datadog UI flagged verify-at-build.

### Changed
- (2026-06-24) Updated the Milestone 8 design brief in the observability meta-PRD to reflect what Michael completed vs. what remains for Whitney. Michael's half is done: ~60 per-attendee Datadog trial orgs manually provisioned (`pool.csv` as source of truth), attendees get their credentials via `merge_pool.py` + success page, 1Password shared vault set up for Whitney/Michael access, `datadog-secret` consumers wired in `otel-collector.yaml` and `falcosidekick.yaml`, ESO + EKS Pod Identity substrate ready. Whitney's remaining work is narrowed to two open questions — whether `app-key` is needed in the cluster secret, and the "org fork" question (attendee's own trial org vs. shared visibility org vs. both) — before writing the ESO `ExternalSecret` that auto-injects per-cluster secrets at fleet spawn time. Teardown settled as manual trigger. Five new Decision Log entries recorded. Research spike for org provisioning removed as a prerequisite (manual provisioning made it unnecessary).
- (2026-06-24) Corrected `gen_ai.operation.name` value in the guard-proxy instrumentation spec (issue #19) and the meta-PRD Milestone 2 attribute inventory. The original value `"sanitize"` is not in the OTel GenAI semconv enum and Datadog only classifies spans as `llm` kind — enabling the before/after messages panel in LLM Observability — for `generate_content`, `chat`, `text_completion`, or `completion`; using a custom value would silently prevent the re-leak trap demo beat from rendering correctly. Changed to `"chat"`. Also corrected issue #19 to mark Step 4 pseudocode as illustrative (the real proxy functions are `blocklisted()`, `input_allowed()`, `moderate()` for input; `output_scrub()` / `_scrub_response()` for output — `sanitize()` and `original_text` do not exist in proxy.py) and added an endpoint reconciliation warning: `guard-proxy.yaml` line 66-67 currently names `monitoring` namespace and the Helm-generated service name `otel-collector-opentelemetry-collector`, while the PRD and research target `otel-collector.observability.svc.cluster.local`; both differ and must be reconciled before spans will export.
- (2026-06-24) Closed issue #18 (evil-mcp-shim instrumentation spec): shim stays dark with no OTel instrumentation; remaining work (add `apply_optimization` to `gitops/ai-layer/server.py`) is untracked and handled by Michael as part of the M2 build. Meta-PRD Decision Log updated; M2 Step 0 untracked-work note corrected to remove the UST env vars item (already in MVP PRD).
- (2026-06-24) Revised evil-mcp-shim instrumentation plan: decided to leave the shim dark (no OTel instrumentation). The ADK agent's `execute_tool` spans already expose the rogue-call chain from the caller side; the genuinely interesting data (poisoned tool description, fake credential response) has no standard OTel semconv home; instrumenting the shim would imply an attacker helpfully traces themselves, undermining the demo narrative. Issue #18 scope reduced to: reconcile the drifted `gitops/ai-layer/server.py` copy (add `apply_optimization` with hardcoded fallback) and add UST env vars to `gitops/ai-layer/resources.yaml`. Cascaded to meta-PRD M2/M3 Step 0 references and M2 Weaver attribute inventory; cascaded file-path correction to MVP PRD Step 4. Decision Log row added to meta-PRD.
- (2026-06-24) Filed per-app OTel instrumentation spec issues for evil-mcp-shim (issue #18) and guard-proxy (issue #19). Both specs lock the pattern decided 2026-06-24: `opentelemetry-api` only in the image (or injected via Operator for the ConfigMap-based guard-proxy), no SDK or exporter in app code, manual spans using OTel semantic conventions. evil-mcp-shim gets `execute_tool` spans with `gen_ai.*` attributes; guard-proxy gets a manual HTTP SERVER span plus a `sanitize` child span capturing redacted before/after content for the re-leak trap beat. References added to M2 and M3 in the meta-PRD.
- (2026-06-24) Filed research spike issue #17 (`research/34-otel-config-dedup-llmobs-activation-2026.md`) covering four open questions that block Milestone 2 and Milestone 5 design conversations: how to point each compiled-language stack component's built-in OTLP exporter at the Collector; whether components emitting both OTel and Prometheus metrics double-bill and how to remedy it; what Datadog-side configuration unlocks the LLM Observability UI beyond receiving `gen_ai.*` spans; and whether `spec.features.apm.enabled: false` is safe when OTel traces reach APM via the Collector path. Updated the meta-PRD to gate M2 and M5 on this spike.
- (2026-06-24) Filed per-app OTel instrumentation specs for evil-mcp-shim (issue #18) and guard-proxy (issue #19), then revised both against the actual OTel semantic conventions spec. evil-mcp-shim: `execute_tool {tool_name}` SERVER spans using `gen_ai.operation.name`, `gen_ai.tool.name`, `gen_ai.tool.type`, `gen_ai.tool.call.id`, and `gen_ai.tool.call.input` (opt-in); UST env vars added to `k8s-manifest.yaml`; W3C context propagation through FastMCP flagged as an investigation task. guard-proxy: HTTP SERVER spans with `http.request.method`, `url.scheme`, `url.path`, `http.response.status_code`; W3C context both extracted (incoming) and injected (outbound to agentgateway); `sanitize` INTERNAL child span captures before/after content via `gen_ai.input.messages` and `gen_ai.output.messages` in the OTel GenAI messages schema (`[{"role":"user","parts":[{"type":"text","content":"..."}]}]`) — the original unredacted text goes on the input side, Datadog SDS is the backstop. All attributes are OTel community semconv; no custom attributes remain. Meta-PRD M2 Decision 6 updated with a pre-decided attribute inventory for both services so the Weaver milestone inherits rather than re-derives the span schema. Meta-PRD M3 updated to reflect the `gen_ai.input.messages`/`gen_ai.output.messages` approach and to close the `OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT` question (irrelevant for hand-written spans).
- (2026-06-24) Research spike #17 → `research/34-otel-config-dedup-llmobs-activation-2026.md`: per-component OTel export config (Q1, all 7 OTel-capable components wired at the Collector with mechanism/value/protocol/propagation/auth/verification), OTel+Prometheus deduplication (Q2, per-component classification + remedy; refutes "accept both, Datadog dedups" on a billing-cardinality argument), Datadog LLM-Obs UI activation (Q3, `ml_app` auto-derived from `service.name`, no DatadogAgent CR key; minimum span shape = recognized `gen_ai.operation.name` + `service.name` + semconv v1.37+; the contrib-exporter→LLM-Obs routing is the one verify-at-build gap), and Q4 confirming `spec.features.apm.enabled: false` is GO (OTel traces reach APM Traces, Service Map, and `trace.*` metrics via the Collector datadog exporter + `datadog/connector`, no Agent APM intake). Triple-pass adversarial validation (official-docs / config-schema / completeness) + final confirm: 36 confirmed, 0 refuted, 6 verify-at-build; all three lenses GO, no locked decision contradicted. Four official-docs corrections web-verified and applied (meta.model_name mapping, OTLP protocol default, Agent APM UDS path, datadog/connector key path). AI-isms clean (0 em-dashes/1k).
- (2026-06-24) provisioning.agenticburn.com: added a "Sponsored by Datadog" / "Sponsored by Accenture" co-sponsor strip to the distributor footer and reblended the palette to Datadog purple (#632CA6) + Accenture violet (#A100FF); deployed live to Railway. 13 lab-distribution tests green.
- (2026-06-24) Backstage image dependabot sweep: pinned patched versions of the 9 fixable transitive deps via yarn resolutions + relock (undici/uuid/tar/prismjs/js-yaml/file-type 21.3.2/@octokit ×3), clearing 21 alerts; dismissed the one unfixable alert (elliptic 6.6.1, GHSA-848j-6mx2-7j84, no patch exists, low-sev browser-bundle polyfill) as tolerable_risk. Merged the two open dependabot PRs (#6, #16).
- (2026-06-24) Completed the Milestone 3 design conversation for the observability plan: locked four decisions covering how to make the workshop's security beats observable in Datadog. guard-proxy gets a `sanitize` INTERNAL child span with `gen_ai.operation.name="chat"` (required for Datadog to render the Input/Output panel in LLM Observability), capturing the original and sanitized prompt via `gen_ai.input.messages`/`gen_ai.output.messages` gated on `OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT` (the proxy must read this env var explicitly — it does not govern hand-written spans automatically). evil-mcp-shim stays dark; Beat 3's rogue tool-call story comes from ADK's native `execute_tool` spans. The re-leak-trap demo is a two-act beat: Act 1 arms content capture so the original prompt leaks into Datadog LLM Observability; Act 2 applies a Collector OTTL transform overlay that replaces the sentinel with `[DEMO-REDACTED]`, demonstrating that Collector-side redaction prevents the leak. Produced child PRD #22 (`prds/22-security-beats-observability.md`). ROADMAP and meta-PRD Decision Log updated.
- (2026-06-24) Locked five architecture decisions for the observability plan that override earlier research spike conclusions: Datadog Agent installs via the Datadog Operator (not Helm chart) — standard ArgoCD sync-wave ordering handles the CRD-before-CR requirement; the Agent's Prometheus scraping is enabled so cert-manager, ESO, Falco, and Falcosidekick (which emit Prometheus only) produce any telemetry at all; the Agent's APM flag is off since OTel traces reach the Datadog APM UI through the Collector path regardless; the OTel Operator is deployed to inject the full Python SDK into custom app pods at startup, keeping images slim (avoids ~20MB grpcio bloat in custom images); and all custom Python/Node.js apps carry only `opentelemetry-api` in their image (no-op until the Operator fires), with manual spans using OTel semantic conventions. Correction notices added to research/32 and research/33 flagging which conclusions are superseded.
- (2026-06-24) agentgateway IN + AI-layer canonical reconciliation. Staged `gitops/ai-layer/agentgateway.yaml` as the canonical deployed agentgateway (ported from `agent/gateway/`; namespace via kustomization, service FQDNs →`agent`, v1.3.0 tracing in the config file per research/29), intentionally NOT yet in `kustomization.yaml` resources so it cannot crash-loop live clusters before its 5 verify-at-build blockers resolve. Added `OTEL_RESOURCE_ATTRIBUTES` (Datadog UST) to the gitops guard-proxy Deployment; repointed `verify/test_observability.py` UST asserts and the STACK-WALKTHROUGH/BUILD-SPEC/cost docs at the canonical gitops copies; added RECONCILIATION headers to the Beat-3 `mcp-authz` toggle manifests + script flagging the whole-config-replace trap (drops tracing + request promptGuard) and the ATTENDEE_NAMESPACE→`agent` resolution. 19 verify + 13 lab-distribution tests green.
- (2026-06-24) lab-distribution v2 (`lab-distribution/`): the distributor now hands each attendee their **console URL** (`a-<id>.agenticburn.com`, primary), **Datadog** dashboard + login + api/app keys, and **AWS** keys (terminal-only), all keyed off email. Extended `pool.csv` to the full schema; success page + email reordered (console → Datadog → AWS). Removed the inherited KodeKloud `/browser` path (routes, table, templates, admin stats) — EKS/console-only. Replaced the stale KCD-Texas/Claude-Code flow (git clone, `spec/BUILD-SPEC.md`) with the console flow. Added `scripts/merge_pool.py` to join the Terraform AWS-cluster pool with Tara's Datadog `generate_accounts_csv.sh` output into `pool.csv`. 13 tests green; offline render-gate suite unaffected. (Phase 4 of `docs/attendee-access-design.md`.)
- (2026-06-23) Research spike #15 → `research/33-otel-sdk-delivery-2026.md`: OTel SDK delivery strategy for the full stack. Decision: do NOT adopt the OTel Operator — single standalone Collector leaves nothing for its Collector half, and guard-proxy/evil-mcp-shim are stdlib `http.server` scripts that OTel Python has no server instrumentation for (injection yields only an outbound `urllib` span). Plan: per-component manual OTel SDK spans (evil-mcp-shim M2, guard-proxy M3); kagent/agentgateway stay config-only; Prometheus-only emitters scraped by the Collector. Triple-pass adversarial validation (official-docs / version+YAML / completeness) + final confirm; GO, 0 refuted. research/24 architecture treated as locked.
- (2026-06-23) Research spike #14 → `research/32-datadog-agent-install-eks-2026.md`: Datadog Agent install method + feature flags for EKS. Decisions: Helm chart (`datadog/datadog`) as one ArgoCD Application (not Operator/EKS-add-on/manual); logs/container collection opt-in, APM OTLP receiver not needed (Collector exports directly), process collection off; no AWS API access needed (Pod Identity if ever, not IRSA); Cluster Agent enabled (within research/24 §2.3 sizing); EKS+CloudWatch integration not needed. Adversarially validated (15 confirmed, 2 inline fixes). research/24 architecture + sizing treated as locked.
- (2026-06-23) RE-RAN research spikes #9–#12 (`research/28–31`, updated in place) per Whitney's updated issues: verified the Datadog **"Agent Observability"** product rename (surface-only — `/llm_observability/` paths, `dd-otlp-source=llmobs`, v1.37+ `gen_ai.*` OTLP ingest unchanged) and the built-in **Sensitive Data Scanner** (scans Agent-Obs traces incl. LLM inputs/outputs, instrumentation-agnostic, default managed group). Re-validated; inline corrections (otel sub-page is not titled "Agent Observability"; SDS is active-by-default for Agent-Obs). #12 conclusion: SDS applies to OTLP/manual spans but SDK span-processors do not — **Collector-side symmetric redaction stays the right path for the re-leak trap** (it must show the before-secret, then confirm sanitized). File paths re-posted as comments on #9–#12.
- (2026-06-23) Attendee-access design spec: added the concrete Datadog trial-org provisioning method (Tara's `generate_accounts_csv.sh` from `DataDog/learning-center-lambdas`) + CSV→pool mapping, login-vs-api/app-key distinction, and the ~14-day org-expiry timing constraint (`docs/attendee-access-design.md`).

### Changed
- (2026-06-23) Created three research spike issues (#9, #10, #11, #12) to gate Milestone 2, 3, and 5 design conversations: #9 covers Datadog LLM Observability OTLP ingestion, #10 covers Python AI-layer instrumentation approach toward OTel GenAI semantic conventions, #11 covers per-component telemetry synthesis for all 13 stack components, and #12 covers how to capture before/after prompt text in guard-proxy traces (the re-leak trap beat). Updated the meta-PRD to reference each spike in its respective milestone step and clarified that wire-or-skip decisions belong to the M5 design conversation, not the research deliverable.
- (2026-06-23) Added notes to research spike issues #9 and #12 that "Datadog LLM Observability" may now be part of a broader "Agent Observability" platform; both spikes should verify the current product name. Issue #12 also now covers whether Datadog's built-in Sensitive Data Scanner applies to manually instrumented spans, or whether Collector-side symmetric redaction is still the right path for the re-leak trap. Fixed a stale research file reference in Milestone 7 (community-dashboard survey is now question 5 inside the issue #11 per-component synthesis, not a separate research/28 file). Recorded two new meta-PRD decisions: wire-or-skip moved to the Milestone 5 design conversation, and guard-proxy before/after sanitization confirmed as its own research spike (issue #12).
- (2026-06-23) Corrected the meta-PRD so that Milestone 2 and 3 design conversations reference pre-completed research spikes (issues #9, #10, #12) rather than instructing the implementing AI to run /research itself. The implementing AI now reads the pre-completed output and stops if the issue is not yet complete. Downstream "Done when" checklists and child-PRD step references updated to match (decisions 2-7 and 2-4 respectively, since step 1 is now a reading prerequisite, not a numbered design decision).
- (2026-06-22) Updated observability planning docs and research/23 to reflect that the TypeScript rewrite of guard-proxy is not happening — all Python apps stay Python, spiny-orb instrumentation is off the table, Decision 4 in research/23 closed as superseded.
- (2026-06-23) Completed the Milestone 1 design conversation for the observability plan: locked all cross-cutting decisions (OTel Collector pipeline shape, Unified Service Tagging vocabulary per component, MVP Datadog account model), produced the MVP child PRD (#13) with implementation steps for wiring the Datadog trial account end-to-end, and created `docs/observability-priorities.md` as a living must-have/nice-to-have reference that every future milestone design conversation reads first.
- (2026-06-23) Clarified Weaver's role in the observability plan: it stays in Milestone 2 (not Milestone 1) because gen_ai.* spans don't exist until the migration happens; the OTel community semconv registry provides the upstream gen_ai.* attribute definitions as a Weaver dependency, so no vocab needs to be pre-defined earlier. Fixed two stale Decision Log entries that contradicted this. Also improved the meta-PRD so that "clear context" instructions correctly tell the user to start a new session rather than asking the AI to clear its own context.
- (2026-06-23) Filed two new research spike issues and updated the meta-PRD to gate on them: issue #14 covers Datadog Agent install method and feature flags for EKS (Helm DaemonSet vs. Datadog Operator, IAM, Cluster Agent, GitOps manifest shape) — a prerequisite for the Milestone 5 design conversation; issue #15 covers OTel SDK delivery strategy for the full cluster stack (whether the OTel Operator is the right cluster-wide mechanism given all OTel-emitting components, how it coexists with the standalone OTel Collector, and what the per-pod delivery pattern is for custom Python components) — a prerequisite for the Milestone 2 design conversation. Clarified that guard-proxy has no gen_ai.* to migrate in Milestone 2 (it makes no LLM calls), so its OTel instrumentation belongs to Milestone 3. Updated Milestone 5's end-state goal and decision structure to reflect the Agent install gap. Updated Milestone 3 to note that OTel SDK delivery is decided in Milestone 2 and inherited.
