# ABOUTME: NetworkPolicy allowing the agent layer to send telemetry to the OTel Collector.
# ABOUTME: Egress on OTLP ports 4317/4318 to the monitoring namespace (matched on the collector pod).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: agent-allow-otel-egress
  namespace: agent
spec:
  # Scoped to the kagent agent pod (see agent-egress-allowlist.yaml); the MCP servers are not locked down.
  # Namespace-wide, matching agent-egress-allowlist. Once ANY Egress policy selects a pod, that pod is
  # default-deny-egress, so widening the allowlist to the namespace (issue #108) means every pod in it
  # needs these baseline allows too. Leaving this scoped to workshop-agent would have cut DNS and
  # intra-namespace traffic for the MCP servers the moment the deny widened, turning a security fix into
  # an outage.
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
      ports:
        - protocol: TCP
          port: 4317
        - protocol: TCP
          port: 4318
