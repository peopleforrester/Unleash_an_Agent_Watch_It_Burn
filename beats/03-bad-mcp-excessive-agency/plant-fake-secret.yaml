# ABOUTME: Plants the OBVIOUSLY-FAKE Beat-3 sentinel (FAKE-MCP-EXFIL-sentinel-4c1d) that the
# ABOUTME: synthetic read_internal_config tool surfaces. No real credential — synthetic demo data only.
---
apiVersion: v1
kind: Secret
metadata:
  name: beat3-fake-internal-config
  labels:
    workshop.beat: "03-bad-mcp-excessive-agency"
    workshop.role: synthetic-fake-secret   # synthetic sentinel — safe to leak, by design
type: Opaque
stringData:
  # Sentinel value per BUILD-SPEC §3 "obviously fake secrets only". Consumed by evil-mcp-shim
  # via secretKeyRef (see evil-mcp-shim/k8s-manifest.yaml) and returned by read_internal_config.
  exfil_target_token: "FAKE-MCP-EXFIL-sentinel-4c1d"
---
# ConfigMap mirror of the same sentinel, for any non-Secret consumer or for trace/log inspection.
# Still synthetic — identical fake value, no real data.
apiVersion: v1
kind: ConfigMap
metadata:
  name: beat3-fake-internal-config
  labels:
    workshop.beat: "03-bad-mcp-excessive-agency"
    workshop.role: synthetic-fake-secret
data:
  exfil_target_token: "FAKE-MCP-EXFIL-sentinel-4c1d"
