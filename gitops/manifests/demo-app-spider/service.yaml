# ABOUTME: Service for the demo-app-spider demo application.
# ABOUTME: ClusterIP service exposing port 80 targeting container port 8080.
---
apiVersion: v1
kind: Service
metadata:
  name: demo-app-spider
  namespace: apps
  labels:
    app: demo-app-spider
spec:
  type: ClusterIP
  selector:
    app: demo-app-spider
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
