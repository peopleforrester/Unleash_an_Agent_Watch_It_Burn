# ABOUTME: Service for the demo-app-wombat demo application.
# ABOUTME: ClusterIP service exposing port 80 targeting container port 8080.
---
apiVersion: v1
kind: Service
metadata:
  name: demo-app-wombat
  namespace: apps
  labels:
    app: demo-app-wombat
spec:
  type: ClusterIP
  selector:
    app: demo-app-wombat
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
