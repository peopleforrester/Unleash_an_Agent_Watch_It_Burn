# ABOUTME: Root Backstage catalog file for the Watch It Burn IDP.
# ABOUTME: Defines the top-level system that all components belong to.
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: watch-it-burn-idp
  description: Watch It Burn Internal Developer Platform
  annotations:
    backstage.io/techdocs-ref: dir:.
  tags:
    - kubernetes
    - eks
    - platform
spec:
  owner: platform-team
