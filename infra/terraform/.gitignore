# Terraform working files: never commit state, plans, provider plugins, or lockfile churn.
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Per-account fleet state + provisioning run logs (generated by fleet.sh, never source).
lab-vpc/states/
*.apply.log
*.reapply.log
